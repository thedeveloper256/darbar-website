# ── IDE / Editor ──
.vscode/

# ── OS generated files ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ── Backup & temp files ──
*.bak
*.tmp
*.swp
*~

# ── Logs ──
*.log

# ── Environment / secrets ──
.env
.env.local
.env.*.local

# ── Dependencies (if you add build tools later) ──
node_modules/

# ── Build output (if you add a build step later) ──
dist/
build/